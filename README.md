# My st build
Patches used:
- none

## Installation
```
git clone https://github.com/DefinitelyNotMai/st
cd st
sudo make install
```

## Configuration
Configure st by making changes in "config.h", run "sudo make install" afterwards.
